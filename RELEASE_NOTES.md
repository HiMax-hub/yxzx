# 雁讯咨询贷款 CRM 与业务工作台 · 发布说明

## 版本信息

| 项目 | 内容 |
|---|---|
| **版本号** | v1.0.0（正式版 GA） |
| **构建模式** | `npm run build:prod`（vite build --mode production） |
| **构建时间** | 2026-08-17 |
| **产物目录** | `dist/`（1.9 MB，6 个文件） |
| **运行环境** | 纯前端静态托管（Nginx / CDN / 对象存储），无需后端 |
| **数据层** | 浏览器 localStorage（前缀 `yanxun_crm_v5_`） |

## 发布内容总览

### 一、正式版数据纯净（本次重点）
- **清除全部演示/测试数据**：6 条虚构贷后在贷账户（周振邦/陈秀兰/赵德海/孙伟/李晓琴/黄建国等）已清空；4 条虚构工作台默认待办已清空；客户/进件/外呼均以空态启动。
- **数据版本升级 v4 → v5**：localStorage 前缀升级为 `yanxun_crm_v5_`，旧版本遗留测试数据不再加载。
- **调试日志清理**：移除公海自动回收 `console.info` 调试输出；仅保留必要的错误处理日志（复制失败/脚本读写失败）。
- **安全策略强化**：全部 5 个种子账号 `mustChangePassword=true`，首次登录强制修改默认密码。

### 二、历史修复与功能（自 v0.x 迭代累计）
- **权限越权修复**：非超管账号无法切换至更高权限账号（角色层级 ROLE_LEVEL 校验）。
- **超管密码安全**：启动不再强制覆盖管理员密码，仅当系统无超管时才补建默认账号。
- **公海认领规则生效**：冷却期（24h）+ 每日认领上限（20 个）实时拦截，批量认领自动过滤。
- **费率联动**：新建进件费用率/提成从系统配置动态读取，不再硬编码。
- **AI 能力全家桶**：客户 AI 信用评分（五维实时算法）、AI 异议化解建议浮层、智能话术引导侧栏（开场/需求/拒绝/促成四阶段）、AI 电销副驾。
- **视觉现代化**：全系统浅色调统一、圆角卡片、border-slate-100 + bg-white 层级、语义色标（蓝=金融活跃/橙=预警）一致；移除全部深色块。

### 三、生产配置
- 新增 `.env.production` / `.env.development` / `.env.production.example` 环境变量体系。
- `vite.config.ts`：生产模式关闭 sourcemap、产物内容哈希（支持 CDN 长缓存）、资源内联阈值 4KB。
- `package.json`：版本号 1.0.0、新增 `build:prod` 与 `test:smoke` 脚本。
- 新增 `scripts/smoke-test.ts` 核心业务冒烟测试（14 项断言：AI 评分/异议匹配/提成/合规/权限/意向）。

## 回归测试结果（2026-08-17）

| 检查项 | 结果 |
|---|---|
| TypeScript 类型检查（tsc --noEmit） | ✅ 0 错误 |
| 生产构建（vite build --mode production） | ✅ 10.36s，分包正常无空 chunk |
| 冒烟测试（14 项断言） | ✅ 14/14 通过 |
| 生产预览（vite preview :4173） | ✅ HTTP 200，页面/资源加载正常 |
| 虚构数据残留扫描（dist 产物） | ✅ 0 处 |
| sourcemap 泄露检查 | ✅ 无 .map 文件 |
| 环境变量注入验证 | ✅ yanxun_crm_v5_ 正确注入 |

## 部署要求

1. 部署前在目标机器执行 `cp .env.production.example .env.production` 并按需修改。
2. 部署 `dist/` 至静态托管，配置单页路由回退与 `/assets/` 长缓存（见 README 的 Nginx 参考配置）。
3. 必须使用 HTTPS 部署（登录凭据与客户隐私数据传输）。
4. 上线后立即登录 `himax` 账号修改默认密码，并在「总控后台 - 员工管理」重置其余账号密码。

## 已知限制

- 主 JS chunk 约 966 KB（gzip 237 KB）超过 800 KB 警告阈值，属可接受范围；后续可通过页面级懒加载（React.lazy）进一步拆分。
- 当前为纯前端 localStorage 架构，多设备/多人实时协作依赖「导出 JSON 备份 + 导入恢复」；接入后端 API 时请配置 `VITE_API_BASE_URL`。
