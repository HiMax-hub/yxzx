# 雁讯咨询贷款 CRM 正式版上线检查清单

> 适用版本：v1.0.0 正式版 ｜ 执行时机：发布窗口前 24h 至发布后 72h
> 每条均需勾选（☐ → ☑）并由负责人签名确认。

---

## 一、发布前检查（发布前 24h）

### 1.1 代码与构建
- ☐ `git status` 干净，无未提交改动
- ☐ `npm run lint`（tsc --noEmit）通过，0 错误
- ☐ `npm run test:smoke` 通过（14 项断言全绿）
- ☐ `npm run build:prod` 构建成功，dist/ 产物完整（index.html + 6 个 assets）
- ☐ 产物中无虚构/演示数据（扫描关键词：周振邦、致电张总 等，应为 0）
- ☐ 产物中无 sourcemap（`ls dist/assets/*.map` 应为空）
- ☐ `grep "yanxun_crm_v5_" dist/assets/*.js` 确认存储前缀已注入

### 1.2 环境与配置
- ☐ `.env.production` 已按实际环境配置（域名/API 网关/存储前缀）
- ☐ `VITE_API_BASE_URL` 若接入后端，已指向生产网关并完成连通性测试
- ☐ 静态托管（Nginx/CDN）配置：单页路由回退 + `/assets/` 30 天长缓存
- ☐ HTTPS 证书已签发且有效（≥ 90 天余量），强制 HTTPS 跳转已配置
- ☐ 安全响应头已配置（nosniff / X-Frame-Options / Referrer-Policy）

### 1.3 账号与权限
- ☐ 生产种子账号清单确认（himax / wangjingli / chenfengkong / zhaocaiwu / zhangqiang）
- ☐ 所有种子账号确认 `mustChangePassword=true`（首次登录强制改密）
- ☐ 员工权限矩阵复核：super_admin > admin > risk/finance > consultant
- ☐ 停用/删除不再需要的测试或临时账号

### 1.4 数据备份（重点）
- ☐ **导出旧版数据备份**：登录旧系统 → 总控后台 → 导出全部业务数据 JSON（客户/进件/用户/配置/贷后/外呼）
- ☐ 备份文件双副本保存：本机 + 异地（网盘/对象存储），文件名含日期（如 `yanxun-backup-20260817.json`）
- ☐ 记录备份文件 SHA-256 校验和，发布后比对
- ☐ 确认备份 JSON 可被系统「导入恢复」功能正确还原（先在测试环境验证一次）

---

## 二、发布执行

### 2.1 发布窗口
- ☐ 选择业务低峰期（建议 21:00 后或周末）发布，避开月末结算/放款日
- ☐ 发布窗口预留 ≥ 2h 缓冲，并知会全部业务负责人

### 2.2 部署步骤
1. ☐ 上传 `dist/` 到目标服务器（scp/rsync/流水线发布均可）
2. ☐ 备份线上旧版本目录（`mv dist dist_bak_20260817`，至少保留 3 个历史版本）
3. ☐ 放置新版本 `dist/`，校验 index.html 与 assets 完整性
4. ☐ 检查 Nginx 配置 reload 成功（`nginx -t && nginx -s reload`）
5. ☐ 冒烟访问：`curl -sI https://域名/` 返回 200，且响应头含安全头

---

## 三、发布后验证（发布后 2h 内）

### 3.1 功能冒烟（按角色登录验证）
- ☐ 超管 `himax` 登录 → 弹窗强制改密 → 改密成功后进入工作台（**首次发布必验**）
- ☐ 工作台：今日待办为空态引导、AI 话术助手、智能话术引导侧栏可打开
- ☐ 客户管理：新建客户 → 列表显示 AI 评分徽章 → 详情显示完整评分卡
- ☐ 跟进记录：输入"客户嫌利息高" → AI 异议化解浮层弹出 → 一键填入
- ☐ 进件管理：新建进件 → 费率/提成与系统配置联动
- ☐ 贷后管理：空态显示正常，无虚构在贷账户
- ☐ 财务结算、产品库、总控后台（员工/公海规则/安全策略）可正常访问

### 3.2 权限回归
- ☐ 顾问账号登录 → 快速切换账号列表仅显示不高于自身权限的账号（越权已修复）
- ☐ 财务账号可看财务结算，但不可进总控后台员工管理
- ☐ 风控账号可看风控视图，不可修改系统配置

### 3.3 数据验证
- ☐ 新用户首次写入数据（新建 1 个测试客户）→ 刷新页面数据仍在（localStorage v5 生效）
- ☐ 若导入旧数据：确认客户/进件/贷后条数与备份一致（抽样比对 ≥ 5 条）
- ☐ 确认旧版本 v4 数据未被误加载（浏览器 localStorage 应出现 `yanxun_crm_v5_*` 键）

---

## 四、回滚方案（出现重大问题时执行）

### 4.1 回滚触发条件
- [ ] 核心功能不可用（登录失败 / 工作台白屏 / 数据写入丢失）
- [ ] 数据完整性受损（客户数据丢失 / 金额错乱）
- [ ] 安全事件（越权访问 / 数据泄露）
- [ ] 性能严重劣化（页面加载 > 5s 持续 30min）

### 4.2 回滚步骤
1. **立即止血**：Nginx 切换回 `dist_bak_20260817`（或 CDN 回退至上一版本对象）
2. **数据保全**：立即导出当前版本 localStorage 数据（防止新数据丢失）
3. **回滚确认**：`curl -sI https://域名/` 200 + 页面指纹与旧版本一致
4. **问题记录**：记录触发原因、影响范围、已修复时间，48h 内出具复盘报告
5. **数据修复**：如因升级造成数据异常，使用备份 JSON 恢复（导入前先本地校验）

### 4.3 回滚保护措施
- ☐ 线上保留最近 3 个历史版本目录（dist_bak_*）
- ☐ 发布说明（RELEASE_NOTES.md）随版本归档，便于定位差异
- ☐ 每次发布前创建可恢复点：备份数据 + 旧版本目录 + 配置快照

---

## 五、监控与告警（上线后持续）

### 5.1 基础设施监控
- ☐ 站点可用性：每 5 分钟探活（HTTP 200），连续 3 次失败触发告警
- ☐ HTTPS 证书到期提醒（提前 30 天告警）
- ☐ 静态资源 4xx/5xx 错误率监控（≥ 1% 告警）

### 5.2 业务运行监控
- ☐ **登录失败告警**：同账号连续 5 次失败 → 即时告警（暴力破解防护，系统已内置锁定）
- ☐ **数据备份提醒**：每周自动提醒导出数据备份（系统当前为手动导出，需人工执行）
- ☐ 外呼合规告警：单号单日外呼超限（系统已内置拦截，监控日志确认拦截记录正常产生）
- ☐ 敏感词拦截审计：定期抽查违规话术拦截日志（承诺批贷等红词）

### 5.3 告警通知
- ☐ 告警渠道确认（企业微信/邮件/短信），值班人名单与联系方式已登记
- ☐ 告警级别分级：P0（立即处理≤30min）/ P1（2h 内）/ P2（24h 内）

---

## 六、发布后 72h 观察期

- ☐ 观察登录/操作是否出现异常报错（浏览器控制台错误率）
- ☐ 收集业务反馈：电销外呼流程、客户录入流程是否有使用障碍
- ☐ 确认首次登录强制改密无卡点（改密后能正常进入系统）
- ☐ 确认无旧版数据残留投诉（员工反映"数据不见了"需解释 v4→v5 升级策略）
- ☐ 观察期结束无 P0/P1 问题 → 正式宣布上线完成，归档检查清单

---

## 执行确认

| 环节 | 负责人 | 时间 | 签名 |
|---|---|---|---|
| 发布前检查 | | | |
| 数据备份 | | | |
| 发布执行 | | | |
| 发布后验证 | | | |
| 回滚预案确认 | | | |
| 监控告警配置 | | | |
| 上线宣布 | | | |
